{
  "name": "headless-monkey",
  "private": true,
  "version": "0.1.0",
  "description": "Headless CMS monorepo (Vite client + Express server)",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "test": "pnpm -r test",
    "build": "pnpm -r build"
  }
}
